const SearchBar = () => {
  return (
    <div className='app-container'>
      <h2>Movie Search App</h2>
      <div>
        <label>
          <input type='text' />
          <button>Search</button>
        </label>
      </div>
    </div>
  );
};

export default SearchBar;
